import { styled } from "styled-components";

export const SubscribeHeading3 = styled.h3`
  font-size: 1rem;
  margin: 0;
`;

export const Content = styled.p`
  white-space: pre-line;
  font-size: 0.75rem;
  color: ${({ theme }) => theme.palette.gray_deep};
  line-height: 1.2;
  margin: 0.5rem 0;
`;
